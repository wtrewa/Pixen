export enum BookingType {
  HOURLY = 'HOURLY',         // e.g. 2hr birthday party
  MULTI_DATE = 'MULTI_DATE', // e.g. Tilak + Engagement + Marriage on separate days
  DATE_RANGE = 'DATE_RANGE', // e.g. 3-day destination wedding package
  FIXED = 'FIXED',           // e.g. Single specific time slot
}